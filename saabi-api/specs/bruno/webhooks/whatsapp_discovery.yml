info:
  name: whatsapp_discovery
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/webhooks/twilio/whatsapp"
  body:
    type: formUrlEncoded
    formUrlEncoded:
      - name: From
        value: "whatsapp:{{phone_number}}"
      - name: Body
        value: "find a plumber in Yaba"
      - name: MessageSid
        value: "SMdisc000000000000000000000000000"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
